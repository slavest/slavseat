import React from 'react';
import { IconBaseProps } from 'react-icons';
import { AiOutlineUser } from 'react-icons/ai';

export function ProfileIcon(props: IconBaseProps) {
  return <AiOutlineUser {...props} />;
}
