export const comparePath = (origin: string, target: string) => {
  return origin === target;
  // const originArr = origin.split('/');
  // const targetArr = target.split('/');
  // return originArr.every((item, index) => item === targetArr[index]);
};
